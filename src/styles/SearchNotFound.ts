import styled from "styled-components";
import { colors } from "../globalStyles";

const NotFoundWarn = styled.span`
    display: block;
    text-align: center;
    font-size: 18px;
    color: ${colors.red};
`

export default NotFoundWarn;
